{
  "name": "@slideops/utils-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A small, purpose-named set of runtime helpers for the SlideOps frontend. Kept intentionally tiny.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
