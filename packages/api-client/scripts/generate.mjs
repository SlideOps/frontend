/*
 * Placeholder for the typed client generation step.
 *
 * Once the backend publishes api/openapi.yaml, this script will read that
 * contract and emit the request and response types into src/generated. For now
 * it explains what it will do and exits cleanly so `make api-client` is safe to
 * run at any time.
 */
console.log('SlideOps api-client: generation is not wired up yet.');
console.log('It will read the backend api/openapi.yaml and emit typed operations into src/generated.');
process.exit(0);
