{
  "name": "@slideops/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The typed client for the SlideOps backend: a fetch wrapper with auth and error normalization, plus a websocket stream helper. Types will later be generated from the backend openapi contract.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "generate": "node ./scripts/generate.mjs"
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
