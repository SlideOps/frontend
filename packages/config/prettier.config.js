/** Shared Prettier configuration for the SlideOps frontend. */

/** @type {import('prettier').Config} */
const config = {
  printWidth: 100,
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  arrowParens: 'always',
  tabWidth: 2,
};

export default config;
