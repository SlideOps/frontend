/*
 * Shared marketing content constants. The sign up and sign in calls point at the
 * unified auth routes inside this same application, since the marketing site, the
 * operator area, and the admin area are now one app served from one origin.
 */

export const signUpUrl = '/register';
export const signInUrl = '/login';

export const tagline = 'Knowledge becomes Capabilities. Capabilities create Confidence.';
