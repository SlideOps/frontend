# The base path for backend API calls. The client sends cookies with every
# request, so in normal deployment the app and the API share one origin and the
# default below is correct. Override it only when the API lives elsewhere.
#
# Default (same origin): /api/v1
VITE_API_BASE=/api/v1
